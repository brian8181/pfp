{
    "version": "2.0.0",
	"tasks": [
	{
		"label": "MAKE DEBUGGDB",
		"type": "shell",
		"command": "make -f ${workspaceFolder}/Makefile",
		"problemMatcher": [
			"$gcc"
		],
		"group": {
			"kind": "build",
			"isDefault": true
		}
	},
	{
		"label": "echo",
		"type": "shell",
		"command": "echo ${workspaceFolder}"
	}
    ]
}
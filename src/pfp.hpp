#ifndef _pfp_HPP
#define _pfp_HPP

#include <iostream>
#include <string>
#include <vector>
#include <stack>
#include <vector>

using std::string;
using std::vector;

// Options Flags
unsigned char DEFAULTS = 0;
unsigned char option_flags = 0;

#endif